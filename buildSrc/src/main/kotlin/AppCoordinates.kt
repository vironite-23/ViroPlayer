/**
 * Application identity, mirrors the template's `AppCoordinates` object.
 */
object AppCoordinates {
    const val APP_ID = "com.vironite.virogallery"
    const val VERSION_NAME = "0.1.0"
    const val VERSION_CODE = 1
}
