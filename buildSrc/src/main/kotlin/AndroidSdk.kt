/**
 * Single source of truth for SDK levels across every Android module.
 * Mirrors the `AndroidSdk` object from the kotlin-android-library-template.
 */
object AndroidSdk {
    const val minSdk = 26
    const val targetSdk = 37
    const val compileSdk = 37
}
